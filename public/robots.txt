# Legibility robots policy. Legibility is agent-native, so AI answer engines and crawlers
# are explicitly welcome: we want to be read, quoted, and cited.
#
# NOTE ON GROUPING: robots.txt groups are NOT additive. A crawler obeys only the most
# specific group that matches its token, so a named group must repeat every rule it needs.
# The AI crawlers below are therefore declared as ONE group (consecutive User-agent lines
# share the rules that follow) and carry their own Disallow.

User-agent: *
Allow: /
Disallow: /dashboard

# AI answer engines and training/search crawlers (GEO): explicitly allowed, same
# dashboard exclusion as everyone else.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-Web
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: CCBot
User-agent: Bytespider
Allow: /
Disallow: /dashboard

Sitemap: https://legibility.io/sitemap.xml
# LLM-oriented site summary (GEO): https://legibility.io/llms.txt
